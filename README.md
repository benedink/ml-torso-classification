
  # Camera Live Feed UI

  This is a code bundle for Camera Live Feed UI. The original project is available at https://www.figma.com/design/8aGfs57skxK0LWBqTqKkP9/Camera-Live-Feed-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  